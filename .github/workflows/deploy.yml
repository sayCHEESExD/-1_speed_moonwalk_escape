# Deploy to Bloxity Hosting.
#
#   dev            -> Bloxity "dev" channel
#   main / master  -> Bloxity "prod" channel
#
# Two halves, because the game is two things: a Colyseus server that runs as a
# long-lived container, and a static Vite build. The server image goes to GHCR
# and is rolled by the Legion control plane; the client is built and uploaded
# to Bloxity Hosting as a zip.
#
# The client build is deliberately NOT gated on the server deploy. A static
# bundle that failed to upload leaves players on the previous one, which is a
# far better outcome than a half-deployed pair.
#
# BOTH ROUTES BELOW WERE VERIFIED AGAINST THE LIVE API before being written,
# each with a negative control, so nothing here is a guess:
#
#   POST legion.bloxity.io/v1/apps/{id}/deploy              -> 401 (exists)
#   POST legion.bloxity.io/v1/apps/{id}/not-a-route         -> 404 (control)
#   POST api.bloxity.io/v1/hosting/games/{id}/frontend      -> 401 (exists)
#   POST api.bloxity.io/v1/hosting/games/{id}/client        -> 404 (control)
#
# The 401s prove the route exists and wants a token; the 404s prove the API
# really does 404 a path it does not serve, so a 401 is signal rather than a
# catch-all.
name: Deploy to Bloxity

on:
  push:
    branches: [dev, main, master]
  workflow_dispatch:
    inputs:
      channel:
        description: Channel to deploy to
        required: true
        default: dev
        type: choice
        options: [dev, prod]

concurrency:
  # One deploy per branch at a time. Two overlapping runs would race to publish
  # the same channel and the winner would be whichever finished last, not
  # whichever was newer.
  group: bloxity-deploy-${{ github.ref }}
  cancel-in-progress: true

env:
  # The Bloxity app this repository deploys to.
  #
  # NOT `animal-obby-escape`: that id belongs to the previous game in this
  # series and is live on both channels right now. Deploying this repo under it
  # would replace that game's server.
  BLOXITY_GAME_ID: speed-moonwalk-escape

  # The repository is named "-1_speed_moonwalk_escape", and a Docker reference
  # may not begin with a dash - so the image is named explicitly rather than
  # derived from the repo. The shape still matches what the docs expect:
  # ghcr.io/<owner>/<game>-server.
  IMAGE_NAME: ghcr.io/saycheesexd/speed-moonwalk-escape-server

  # The room's player cap, and the same number as MAX_PLAYERS_PER_ROOM in
  # shared/src/constants/network.ts. Legion fills a pod to seatCap and then
  # spawns the next one, so this MUST equal the Colyseus room's maxClients: a
  # larger figure here would have the matchmaker send a sixteenth player to a
  # room that refuses them.
  SEAT_CAP: '15'
  # The pod ceiling, so total capacity is 15 x 5 = 75 concurrent players.
  MAX_REPLICAS: '5'

  # Two DIFFERENT hosts, and that is not a typo: the control plane that rolls
  # container images is legion.bloxity.io, while the static-frontend upload
  # lives on api.bloxity.io. Overridable as repository variables so a change on
  # Bloxity's side stays a settings edit rather than a commit.
  LEGION_API: ${{ vars.LEGION_API_BASE || 'https://legion.bloxity.io' }}
  HOSTING_API: ${{ vars.HOSTING_API_BASE || 'https://api.bloxity.io' }}

jobs:
  # Which channel this run publishes to, decided once so both jobs agree.
  channel:
    runs-on: ubuntu-latest
    outputs:
      name: ${{ steps.pick.outputs.name }}
    steps:
      - id: pick
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            CHANNEL="${{ inputs.channel }}"
          elif [ "${{ github.ref_name }}" = "dev" ]; then
            CHANNEL="dev"
          else
            CHANNEL="prod"
          fi
          echo "name=$CHANNEL" >> "$GITHUB_OUTPUT"
          echo "Deploying branch '${{ github.ref_name }}' to the '$CHANNEL' channel."

  # ------------------------------------------------------------------ server
  server:
    needs: channel
    runs-on: ubuntu-latest
    permissions:
      contents: read
      # Required to push to ghcr.io with the built-in GITHUB_TOKEN.
      packages: write
    steps:
      - uses: actions/checkout@v4

      - uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push the server image
        uses: docker/build-push-action@v6
        with:
          # The repository root: the Dockerfile installs the whole npm
          # workspace because the server imports @moonwalk/shared.
          context: .
          file: ./Dockerfile
          push: true
          # Two tags: an IMMUTABLE "<channel>-<sha>", which is what gets
          # deployed, and a moving "<channel>" pointer for humans reading the
          # package list. Deploying the moving one would mean a re-run could
          # roll out whatever a later push had since overwritten.
          tags: |
            ${{ env.IMAGE_NAME }}:${{ needs.channel.outputs.name }}-${{ github.sha }}
            ${{ env.IMAGE_NAME }}:${{ needs.channel.outputs.name }}
          # Stamped into the image so /health can name the commit it is running.
          # The client reports its own, and the two being different is the
          # fault that took a day to find the first time it happened.
          build-args: |
            BUILD_VERSION=${{ github.sha }}
          # A layer cache in the registry, so an unchanged lockfile does not
          # re-run "npm ci" on every push.
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Roll the server on Legion
        env:
          TOKEN: ${{ secrets.LEGION_DEPLOY_TOKEN }}
          CHANNEL: ${{ needs.channel.outputs.name }}
        run: |
          set -euo pipefail
          if [ -z "${TOKEN:-}" ]; then
            echo "::error::LEGION_DEPLOY_TOKEN is not set. Add it under Settings -> Secrets and variables -> Actions."
            exit 1
          fi

          IMAGE="${IMAGE_NAME}:${CHANNEL}-${GITHUB_SHA}"
          URL="${LEGION_API}/v1/apps/${BLOXITY_GAME_ID}/deploy"

          # seatCap and maxReplicas are numbers, not strings - hence --argjson.
          # No port is sent: Legion injects PORT and the server reads it.
          # `version` is the commit SHA, which is what makes a deployment
          # immutable and traceable back to a single push.
          PAYLOAD=$(jq -n \
            --arg channel "$CHANNEL" \
            --arg image "$IMAGE" \
            --arg version "${GITHUB_SHA}" \
            --argjson seatCap "${SEAT_CAP}" \
            --argjson maxReplicas "${MAX_REPLICAS}" \
            '{channel: $channel, image: $image, version: $version, seatCap: $seatCap, maxReplicas: $maxReplicas}')

          echo "POST $URL"
          echo "$PAYLOAD" | jq .

          HTTP=$(curl -sS -o /tmp/deploy.out -w '%{http_code}' \
            -X POST "$URL" \
            -H "Authorization: Bearer ${TOKEN}" \
            -H 'Content-Type: application/json' \
            --data "$PAYLOAD") || true

          echo "--- response ($HTTP) ---"
          cat /tmp/deploy.out || true
          echo

          case "$HTTP" in
            2*) echo "Server rolled on '$CHANNEL' with seatCap ${SEAT_CAP}, maxReplicas ${MAX_REPLICAS}." ;;
            401|403)
              echo "::error::Legion rejected the token. Re-copy LEGION_DEPLOY_TOKEN from My Games on hosting.bloxity.io (the eye icon)."
              exit 1 ;;
            404)
              echo "::error::No such app on Legion - check that the game id '${BLOXITY_GAME_ID}' matches the one created on hosting.bloxity.io."
              exit 1 ;;
            *)  echo "::error::Server deploy failed with HTTP $HTTP."; exit 1 ;;
          esac

      # ------------------------------------------------------------------
      # Did the container ACTUALLY change?
      #
      # The call above only proves Legion accepted the request. It accepted six
      # of them while the same container kept running, and nothing anywhere
      # said so: the client deployed fine each time, so the two halves drifted
      # apart until the running server was old enough to hang up on a message
      # the new client sends - which players experienced as "everyone is called
      # Guest and nobody levels up".
      #
      # /health reports the commit it was built from, so the rollout can be
      # waited on and FAILED rather than assumed.
      - name: Wait for the server to actually be running this commit
        env:
          CHANNEL: ${{ needs.channel.outputs.name }}
        run: |
          set -euo pipefail
          if [ "$CHANNEL" = "prod" ]; then
            HOST="https://${BLOXITY_GAME_ID}.host.bloxity.io"
          else
            HOST="https://${BLOXITY_GAME_ID}.dev.host.bloxity.io"
          fi
          echo "Polling $HOST/health for version ${GITHUB_SHA}"

          # Ten minutes: a cold image pull on a managed host is slow, and a
          # deploy that takes longer than that has gone wrong anyway.
          for attempt in $(seq 1 60); do
            BODY=$(curl -fsS --max-time 10 "$HOST/health" || echo '{}')
            VERSION=$(printf '%s' "$BODY" | sed -n 's/.*"version":"\([^"]*\)".*/\1/p')
            if [ "$VERSION" = "$GITHUB_SHA" ]; then
              echo "Server is running ${GITHUB_SHA}."
              exit 0
            fi
            echo "  attempt ${attempt}/60: running '${VERSION:-unknown}'"
            sleep 10
          done

          echo "::error::Legion accepted the deploy but the server is still NOT running ${GITHUB_SHA}."
          echo "::error::The container is serving '${VERSION:-unknown}'. The usual cause is that Legion"
          echo "::error::cannot PULL ${IMAGE_NAME}: make that GHCR package public (Packages ->"
          echo "::error::speed-moonwalk-escape-server -> Package settings -> Change visibility), or give"
          echo "::error::Legion registry credentials. A client deployed against an older server loses"
          echo "::error::player names and, if the gap is wide enough, the connection itself."
          exit 1

          if [ "$CHANNEL" = "dev" ]; then
            echo "Backend: https://${BLOXITY_GAME_ID}.dev.host.bloxity.io"
          else
            echo "Backend: https://${BLOXITY_GAME_ID}.host.bloxity.io"
          fi

  # ------------------------------------------------------------------ client
  client:
    needs: channel
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          # Matches the engines field and the Netlify build.
          node-version: '20'
          cache: npm

      - run: npm ci

      # The repo's own gates, run before anything is published rather than
      # after: a build that typechecks but fails "verify" is a broken course,
      # and finding that out from players is finding out too late.
      - name: Typecheck and verify
        run: |
          npm run typecheck
          npm run verify
          npm run verify:assets

      - name: Build the client
        env:
          # The Bloxity slug the SDK initialises with - the same app id this
          # workflow deploys to, so the two can never name different games.
          VITE_BLOXITY_GAME_ID: ${{ env.BLOXITY_GAME_ID }}
          # The same commit the server image is stamped with, so a hosted
          # bundle can be identified without reading its JavaScript.
          VITE_BUILD_VERSION: ${{ github.sha }}
          # Baked in at build time - Vite has no later step in which to inject
          # them, so a change here means a rebuild.
          #
          # The channel-to-host mapping is Bloxity's own: ".dev." is the DEV
          # backend and the bare host is PROD. Getting this backwards points
          # dev players at the production server, which is invisible until
          # somebody notices their test progress on the live leaderboard.
          VITE_SERVER_URL: ${{ needs.channel.outputs.name == 'prod' && (vars.SERVER_URL_PROD || 'wss://speed-moonwalk-escape.host.bloxity.io') || (vars.SERVER_URL_DEV || 'wss://speed-moonwalk-escape.dev.host.bloxity.io') }}
        run: |
          set -euo pipefail
          echo "channel=${{ needs.channel.outputs.name }}"
          echo "VITE_SERVER_URL=$VITE_SERVER_URL"
          npm run build:client

      - name: Check the build against the 12 MB budget
        run: npm run size:client

      - name: Package the client
        run: |
          set -euo pipefail
          test -f client/dist/index.html || { echo "::error::client/dist/index.html is missing - the build produced nothing."; exit 1; }
          # A ZIP, because that is what the hosting upload accepts. Zipped from
          # INSIDE client/dist so index.html sits at the archive ROOT rather
          # than under a "dist" directory the host would serve as a subpath.
          (cd client/dist && zip -qr "${GITHUB_WORKSPACE}/client-dist.zip" .)
          ls -la client-dist.zip
          echo "--- archive root ---"
          unzip -l client-dist.zip
          # Fail loudly if index.html is not at the root: a zip that unpacks a
          # directory serves a blank page and nothing else would catch it.
          unzip -l client-dist.zip | awk '{print $4}' | grep -qx 'index.html' \
            || { echo "::error::index.html is not at the archive root."; exit 1; }

      - name: Keep the bundle on the run
        uses: actions/upload-artifact@v4
        with:
          name: client-${{ needs.channel.outputs.name }}-${{ github.sha }}
          path: client-dist.zip
          retention-days: 14

      - name: Upload the client to Bloxity Hosting
        env:
          TOKEN: ${{ secrets.LEGION_DEPLOY_TOKEN }}
          CHANNEL: ${{ needs.channel.outputs.name }}
        run: |
          set -euo pipefail
          if [ -z "${TOKEN:-}" ]; then
            echo "::error::LEGION_DEPLOY_TOKEN is not set. Add it under Settings -> Secrets and variables -> Actions."
            exit 1
          fi

          # "version" is what the dashboard compares against the deployed
          # backend to tell you whether the two halves match. The commit sha is
          # used for both, so a frontend and a backend from the same push carry
          # the same version string.
          URL="${HOSTING_API}/v1/hosting/games/${BLOXITY_GAME_ID}/frontend?channel=${CHANNEL}&version=${GITHUB_SHA}"
          echo "POST $URL"

          # The zip is the raw request BODY, not a multipart field.
          HTTP=$(curl -sS -o /tmp/upload.out -w '%{http_code}' \
            -X POST "$URL" \
            -H "Authorization: Bearer ${TOKEN}" \
            -H 'Content-Type: application/zip' \
            --data-binary @client-dist.zip) || true

          echo "--- response ($HTTP) ---"
          cat /tmp/upload.out || true
          echo

          case "$HTTP" in
            2*)
              echo "Client published to the '$CHANNEL' channel."
              echo "::notice::Client ${GITHUB_SHA} published to '$CHANNEL'. Verify the SERVER is on the same commit: curl https://speed-moonwalk-escape${CHANNEL:+$([ "$CHANNEL" = dev ] && echo .dev)}.host.bloxity.io/health" ;;
            401|403)
              echo "::error::Bloxity rejected the token. Re-copy LEGION_DEPLOY_TOKEN from My Games on hosting.bloxity.io (the eye icon)."
              exit 1 ;;
            404)
              echo "::error::No such game on Bloxity Hosting - check that '${BLOXITY_GAME_ID}' matches the id created on hosting.bloxity.io."
              exit 1 ;;
            *)  echo "::error::Client upload failed with HTTP $HTTP. The bundle is attached to this run as an artifact."; exit 1 ;;
          esac

          if [ "$CHANNEL" = "dev" ]; then
            echo "Frontend: https://${BLOXITY_GAME_ID}.dev.play.bloxity.io"
          else
            echo "Frontend: https://${BLOXITY_GAME_ID}.play.bloxity.io"
          fi
